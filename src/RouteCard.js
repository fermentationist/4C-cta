import React from "react";

const RouteCard = props => {
    return (
        <div>
            <h1>{props.name}</h1>
            <h2>{props.etas}</h2>
        </div>
    )
}
export default RouteCard;